{
  "name": "puhelinluettelo_backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node index.js",
    "watch": "nodemon index.js",
    "build:frontend": "rm -rf build && cd ../../../fullStackOpen/osa2/puhelinluettelo && npm run build --prod && cp -r build ../../../nodeProjects/fullstackOpen-osa3/puhelinluettelo_backend",
    "deploy": "git push heroku master",
    "deploy:full": "npm run build:frontend && git add . && git commit -m 'frontend build' && git push && npm run deploy",
    "logs:prod": "heroku logs -- tail"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "nodemon": "^1.19.1"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^8.0.0",
    "express": "^4.17.1",
    "mongoose": "^5.6.4",
    "mongoose-unique-validator": "^2.0.3"
  }
}
